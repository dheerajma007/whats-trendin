{
    "name": "whats-trendin",
    "version": "1.0.0",
    "description": "Whats-Trendin",
    "main": "server.js",
    "scripts": {
        "build-client": "cd client && npm install && npm run build",
        "start": "npm install &&  npm run build-client && node server.js",
        "local": "node -r dotenv/config server.js",
        "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.17.1",
        "request": "^2.88.2",
        "request-promise": "^4.2.5",
        "woeid": "^2.0.3"
    },
    "devDependencies": {
        "dotenv": "^8.2.0"
    }
}
